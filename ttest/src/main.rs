#[macro_use]
extern crate serde_derive;

use std::io;
use std::process;
use std::io::Write;

mod blockchain;

fn main(){
    let mut minder_addr=String::new();
    let mut difficulty=String::new();
    let mut choice=String::new();
}